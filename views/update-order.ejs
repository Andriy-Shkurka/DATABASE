<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Редагування замовлення</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <% if (typeof error !== 'undefined' && error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>

        <div class="main-page">
            <h2>Редагування замовлення</h2>

            <% if (orders.length === 0) { %>
                <p>Немає замовлень для редагування.</p>
            <% } else { %>
                <form action="/update-order" method="POST">
                    <label for="order_id">Виберіть замовлення для редагування:</label>
                    <select name="order_id" id="order_id">
                        <% orders.forEach(order => { %>
                            <option value="<%= order.order_id %>">
                                <%= order.order_id %> - <%= order.client_name %> - <%= order.status %>
                            </option>
                        <% }); %>
                    </select><br><br>

                    <label for="repair_type">Тип ремонту:</label>
                    <input type="text" id="repair_type" name="repair_type" value="<%= orders[0].repair_type %>" required><br><br>

                    <label for="price">Ціна:</label>
                    <input type="text" id="price" name="price" value="<%= orders[0].price %>" required><br><br>

                    <button type="submit">Зберегти зміни</button>
                </form>
            <% } %>
            <br>
            <a href="/" class="back-button">Повернутися до меню</a>
        </div>
    </div>
</body>
</html>
